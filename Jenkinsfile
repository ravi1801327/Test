pipeline {
    agent any
    stages {
        stage('Cloning the repository'){
        echo "Cloning the repository"
        }
        stage('Script') {
            echo "Script is running properly "
            }
        }
    }
}
